import {
    Vector2, Vector3,
} from '../../../three/threebuild/three_module.js';
import { expect, test } from 'vitest';
import Player from '../player.js';

test('simple sum', () => {
    expect(1 + 2).toBe(3);
});
